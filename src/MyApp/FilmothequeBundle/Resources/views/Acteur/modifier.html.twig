{% extends "MyAppFilmothequeBundle:default:index.html.twig" %}
{% block title %}Modifier un acteur{% endblock %}
{% block contenu %}
    <h1>Modifier un acteur</h1>

    {% if message %}
        <p>{{ message }}</p>
    {% endif %}

    <form action="" method="post" {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" />
    </form>
    <p><a href="{{ path('myapp_acteur_lister') }}">Retour à la liste des acteurs</a></p>
{% endblock %}